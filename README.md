# Bash-Weather
Developed a Bash script that fetches real-time weather data from the Open Meteo (https://open-meteo.com/en/docs) and provides clothing recommendations to users based on current temperature and precipitation probability. The script is automated via a cron job to run daily at a set time, delivering weather alerts directly in the terminal.
